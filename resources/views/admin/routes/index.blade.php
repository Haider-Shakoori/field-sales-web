<x-layouts.app>
    <div class="mb-6 flex items-center justify-between gap-4"><div><h1 class="text-2xl font-bold">Routes</h1><p class="mt-1 text-sm text-slate-400">Recurring planned customer routes and visit sequence.</p></div>@if(auth()->user()->hasPermission('customers:manage'))<a href="{{ route('admin.routes.create') }}" class="rounded-xl bg-indigo-500 px-4 py-2.5 font-semibold">Add route</a>@endif</div>
    <div class="grid gap-4 lg:grid-cols-2">@forelse($routes as $route)<a href="{{ route('admin.routes.show', $route) }}" class="rounded-2xl border border-white/10 bg-slate-900 p-5 hover:bg-slate-800"><div class="flex items-center justify-between"><h2 class="font-semibold">{{ $route->code }} — {{ $route->name }}</h2><span class="text-xs text-slate-400">{{ $route->is_active ? 'Active' : 'Inactive' }}</span></div><p class="mt-2 text-sm text-slate-400">{{ $route->territory?->name ?? 'No territory' }} · {{ implode(', ', $route->weekdays ?? []) ?: 'No weekdays' }}</p><p class="mt-3 text-sm">{{ $route->customer_memberships_count }} stops</p></a>@empty<div class="rounded-2xl border border-white/10 bg-slate-900 p-8 text-slate-400">No routes found.</div>@endforelse</div><div class="mt-5">{{ $routes->links() }}</div>
</x-layouts.app>
