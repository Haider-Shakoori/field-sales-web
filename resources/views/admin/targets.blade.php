<x-layouts.app>
<div class="space-y-5"><div><h1 class="text-3xl font-black">Targets</h1><p class="mt-1 text-sm text-slate-500">Sales, collections, visits and acquisition goals.</p></div><div class="overflow-x-auto rounded-2xl border border-slate-200 bg-white dark:border-white/10 dark:bg-slate-900"><table class="min-w-full text-sm"><thead class="bg-slate-50 text-left text-xs uppercase tracking-wider text-slate-500 dark:bg-white/5"><tr><th class="p-4">Name</th><th class="p-4">Metric</th><th class="p-4">Scope</th><th class="p-4">Period</th><th class="p-4">Target</th><th class="p-4">Weight</th></tr></thead><tbody class="divide-y divide-slate-100 dark:divide-white/10">@forelse($rows as $row)<tr><td class="p-4 font-semibold">{{ $row->name }}</td><td class="p-4">{{ str($row->metric)->replace('_',' ')->title() }}</td><td class="p-4">{{ $row->scope_type }}</td><td class="p-4">{{ $row->period_start?->format('Y-m-d') }} → {{ $row->period_end?->format('Y-m-d') }}</td><td class="p-4">{{ number_format($row->target_value,2) }}</td><td class="p-4">{{ number_format($row->weight,2) }}%</td></tr>@empty<tr><td colspan="6" class="p-10 text-center text-slate-500">No targets configured yet.</td></tr>@endforelse</tbody></table></div>{{ $rows->links() }}</div>
</x-layouts.app>
