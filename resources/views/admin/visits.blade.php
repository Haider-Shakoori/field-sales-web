<x-layouts.app>
<div class="space-y-5"><div><h1 class="text-3xl font-black">Customer visits</h1><p class="mt-1 text-sm text-slate-500">Planned/unplanned check-ins, geofence verification and outcomes.</p></div><div class="overflow-x-auto rounded-2xl border border-slate-200 bg-white dark:border-white/10 dark:bg-slate-900"><table class="min-w-full text-sm"><thead class="bg-slate-50 text-left text-xs uppercase tracking-wider text-slate-500 dark:bg-white/5"><tr><th class="p-4">Salesman</th><th class="p-4">Customer</th><th class="p-4">Check in</th><th class="p-4">Duration</th><th class="p-4">Plan</th><th class="p-4">Geofence</th><th class="p-4">Outcome</th></tr></thead><tbody class="divide-y divide-slate-100 dark:divide-white/10">@forelse($rows as $row)<tr><td class="p-4">{{ $row->salesman?->user?->name }}</td><td class="p-4 font-semibold">{{ $row->customer?->shop_name ?: $row->customer?->name }}</td><td class="p-4">{{ $row->checked_in_at?->format('Y-m-d H:i') }}</td><td class="p-4">{{ $row->duration_minutes !== null ? $row->duration_minutes.' min' : 'Active' }}</td><td class="p-4">{{ $row->is_planned?'Planned':'Unplanned' }}</td><td class="p-4"><span class="{{ $row->within_geofence?'text-emerald-600':'text-amber-600' }}">{{ $row->within_geofence?'Inside':'Outside' }}</span><div class="text-xs text-slate-500">{{ $row->distance_from_customer_m !== null ? round($row->distance_from_customer_m).' m' : 'No customer coordinates' }}</div></td><td class="p-4">{{ $row->outcome ?? '—' }}</td></tr>@empty<tr><td colspan="7" class="p-10 text-center text-slate-500">No visits yet.</td></tr>@endforelse</tbody></table></div>{{ $rows->links() }}</div>
</x-layouts.app>
