<x-layouts.app>
<div class="space-y-5"><div><h1 class="text-3xl font-black">Collections</h1><p class="mt-1 text-sm text-slate-500">Customer payments with field proof and references.</p></div><div class="overflow-x-auto rounded-2xl border border-slate-200 bg-white dark:border-white/10 dark:bg-slate-900"><table class="min-w-full text-sm"><thead class="bg-slate-50 text-left text-xs uppercase tracking-wider text-slate-500 dark:bg-white/5"><tr><th class="p-4">Customer</th><th class="p-4">Amount</th><th class="p-4">Method</th><th class="p-4">Reference</th><th class="p-4">Collected</th></tr></thead><tbody class="divide-y divide-slate-100 dark:divide-white/10">@forelse($rows as $row)<tr><td class="p-4 font-semibold">{{ $row->customer?->shop_name ?: $row->customer?->name }}</td><td class="p-4 font-semibold">{{ number_format($row->amount,2) }} {{ $row->currency }}</td><td class="p-4">{{ str($row->payment_method)->replace('_',' ')->title() }}</td><td class="p-4">{{ $row->receipt_number ?: $row->manual_reference ?: '—' }}</td><td class="p-4">{{ $row->collected_at?->format('Y-m-d H:i') }}</td></tr>@empty<tr><td colspan="5" class="p-10 text-center text-slate-500">No collections yet.</td></tr>@endforelse</tbody></table></div>{{ $rows->links() }}</div>
</x-layouts.app>
