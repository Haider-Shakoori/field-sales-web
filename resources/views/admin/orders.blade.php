<x-layouts.app>
<div class="space-y-5"><div><h1 class="text-3xl font-black">Orders</h1><p class="mt-1 text-sm text-slate-500">Field orders captured online or offline.</p></div><div class="overflow-x-auto rounded-2xl border border-slate-200 bg-white dark:border-white/10 dark:bg-slate-900"><table class="min-w-full text-sm"><thead class="bg-slate-50 text-left text-xs uppercase tracking-wider text-slate-500 dark:bg-white/5"><tr><th class="p-4">Order</th><th class="p-4">Customer</th><th class="p-4">Salesman</th><th class="p-4">Ordered</th><th class="p-4">Total</th><th class="p-4">Cash / Credit</th><th class="p-4">Status</th></tr></thead><tbody class="divide-y divide-slate-100 dark:divide-white/10">@forelse($rows as $row)<tr><td class="p-4 font-mono text-xs">{{ $row->order_number }}</td><td class="p-4 font-semibold">{{ $row->customer?->shop_name ?: $row->customer?->name }}</td><td class="p-4">{{ $row->salesman?->user?->name }}</td><td class="p-4">{{ $row->ordered_at?->format('Y-m-d H:i') }}</td><td class="p-4 font-semibold">{{ number_format($row->total_amount,2) }} {{ $row->currency }}</td><td class="p-4">{{ number_format($row->cash_amount,2) }} / {{ number_format($row->credit_amount,2) }}</td><td class="p-4">{{ $row->status }}</td></tr>@empty<tr><td colspan="7" class="p-10 text-center text-slate-500">No orders yet.</td></tr>@endforelse</tbody></table></div>{{ $rows->links() }}</div>
</x-layouts.app>
