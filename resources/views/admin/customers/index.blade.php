<x-layouts.app>
    <div class="mb-6 flex flex-wrap items-center justify-between gap-4"><div><h1 class="text-2xl font-bold">Customers</h1><p class="mt-1 text-sm text-slate-400">Customer master data and geofence coordinates.</p></div>@if(auth()->user()->hasPermission('customers:manage'))<a href="{{ route('admin.customers.create') }}" class="rounded-xl bg-indigo-500 px-4 py-2.5 font-semibold">Add customer</a>@endif</div>
    <form class="mb-5"><input name="search" value="{{ $search }}" placeholder="Search name, code, phone…" class="w-full max-w-xl rounded-xl border border-white/10 bg-slate-900 px-4 py-3"></form>
    <div class="overflow-hidden rounded-2xl border border-white/10 bg-slate-900"><div class="overflow-x-auto"><table class="min-w-full text-left text-sm"><thead class="bg-white/5"><tr><th class="px-5 py-3">Customer</th><th class="px-5 py-3">Territory</th><th class="px-5 py-3">Phone</th><th class="px-5 py-3">Status</th><th></th></tr></thead><tbody class="divide-y divide-white/10">
        @forelse($customers as $customer)<tr><td class="px-5 py-4"><div class="font-medium">{{ $customer->code }} — {{ $customer->name }}</div><div class="text-xs text-slate-400">{{ $customer->address }}</div></td><td class="px-5 py-4">{{ $customer->territory?->name ?? 'Unassigned' }}</td><td class="px-5 py-4">{{ $customer->phone ?? '—' }}</td><td class="px-5 py-4">{{ $customer->is_active ? 'Active' : 'Inactive' }}</td><td class="px-5 py-4 text-right"><a href="{{ route('admin.customers.show', $customer) }}" class="rounded-lg bg-white/10 px-3 py-2">View</a></td></tr>@empty<tr><td colspan="5" class="px-5 py-10 text-center text-slate-400">No customers found.</td></tr>@endforelse
    </tbody></table></div></div><div class="mt-5">{{ $customers->links() }}</div>
</x-layouts.app>
