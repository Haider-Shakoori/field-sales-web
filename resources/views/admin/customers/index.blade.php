<x-layouts.app>
    <div class="mb-6 flex flex-wrap items-center justify-between gap-4"><div><h1 class="text-2xl font-bold">{{ __('Customers') }}</h1><p class="mt-1 text-sm text-slate-400">{{ __('Customer master data, credit exposure and geofence coordinates.') }}</p></div>@if(auth()->user()->hasPermission('customers:manage'))<a href="{{ route('admin.customers.create') }}" class="rounded-xl bg-indigo-500 px-4 py-2.5 font-semibold">{{ __('Add customer') }}</a>@endif</div>
    <form class="mb-5"><input name="search" value="{{ $search }}" placeholder="{{ __('Search name, code, phone…') }}" class="w-full max-w-xl rounded-xl border border-white/10 bg-slate-900 px-4 py-3"></form>
    <div class="overflow-hidden rounded-2xl border border-white/10 bg-slate-900"><div class="overflow-x-auto"><table class="min-w-full text-left text-sm"><thead class="bg-white/5"><tr><th class="px-5 py-3">{{ __('Customer') }}</th><th class="px-5 py-3">{{ __('Territory') }}</th><th class="px-5 py-3">{{ __('Outstanding') }}</th><th class="px-5 py-3">{{ __('Phone') }}</th><th class="px-5 py-3">{{ __('Status') }}</th><th></th></tr></thead><tbody class="divide-y divide-white/10">
    @forelse($customers as $customer) @php($balances = $balanceRows->get($customer->uuid)['balances'] ?? []) <tr><td class="px-5 py-4"><div class="font-medium">{{ $customer->code }} — {{ $customer->name }}</div><div class="text-xs text-slate-400">{{ $customer->address }}</div></td><td class="px-5 py-4">{{ $customer->territory?->name ?? __('Unassigned') }}</td><td class="px-5 py-4">@forelse($balances as $balance)<div class="{{ $balance['outstanding_balance'] > 0 ? 'font-semibold text-amber-300' : 'text-slate-400' }}">{{ $balance['currency'] }} {{ number_format($balance['outstanding_balance'], 2) }}</div>@empty<span class="text-slate-500">—</span>@endforelse</td><td class="px-5 py-4">{{ $customer->phone ?? '—' }}</td><td class="px-5 py-4">{{ $customer->is_active ? __('Active') : __('Inactive') }}</td><td class="px-5 py-4 text-right"><a href="{{ route('admin.customers.show', $customer) }}" class="rounded-lg bg-white/10 px-3 py-2">{{ __('View') }}</a></td></tr>
    @empty<tr><td colspan="6" class="px-5 py-10 text-center text-slate-400">{{ __('No customers found.') }}</td></tr>@endforelse
    </tbody></table></div></div><div class="mt-5">{{ $customers->links() }}</div>
</x-layouts.app>
