<x-layouts.app>
<link rel="stylesheet" href="{{ asset('vendor/leaflet/leaflet.css') }}">
<div class="mb-6"><h1 class="text-2xl font-bold tracking-tight">{{ __('Territory heat map') }}</h1><p class="mt-1 text-sm text-slate-400">{{ __('Compare customer coverage, visits, approved sales and verified collections across territories.') }}</p></div>
<form method="GET" class="mb-5 grid gap-3 rounded-2xl border border-white/10 bg-slate-900 p-4 sm:grid-cols-5"><input type="date" name="date_from" value="{{ $from }}" class="rounded-xl border border-white/10 bg-slate-950 px-3 py-2.5"><input type="date" name="date_to" value="{{ $to }}" class="rounded-xl border border-white/10 bg-slate-950 px-3 py-2.5"><select name="metric" class="rounded-xl border border-white/10 bg-slate-950 px-3 py-2.5">@foreach(['sales','visits','collections','customers'] as $item)<option value="{{ $item }}" @selected($metric === $item)>{{ __(str($item)->title()->toString()) }}</option>@endforeach</select><select name="currency" class="rounded-xl border border-white/10 bg-slate-950 px-3 py-2.5">@foreach(['AFN','USD','PKR'] as $item)<option value="{{ $item }}" @selected($currency === $item)>{{ $item }}</option>@endforeach</select><button class="rounded-xl bg-indigo-500 px-4 py-2.5 font-semibold hover:bg-indigo-400">{{ __('Apply') }}</button></form>
<div class="mb-5 grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><div class="rounded-2xl border border-white/10 bg-slate-900 p-4"><p class="text-xs text-slate-500">{{ __('Customers') }}</p><p class="mt-2 text-2xl font-bold">{{ $summary['customers'] }}</p></div><div class="rounded-2xl border border-white/10 bg-slate-900 p-4"><p class="text-xs text-slate-500">{{ __('Visits') }}</p><p class="mt-2 text-2xl font-bold">{{ $summary['visits'] }}</p></div><div class="rounded-2xl border border-white/10 bg-slate-900 p-4"><p class="text-xs text-slate-500">{{ __('Approved sales') }}</p><p class="mt-2 text-2xl font-bold">{{ $currency }} {{ number_format($summary['sales'],2) }}</p></div><div class="rounded-2xl border border-white/10 bg-slate-900 p-4"><p class="text-xs text-slate-500">{{ __('Verified collections') }}</p><p class="mt-2 text-2xl font-bold">{{ $currency }} {{ number_format($summary['collections'],2) }}</p></div></div>
<div class="grid gap-5 xl:grid-cols-[minmax(0,1fr)_400px]"><section class="overflow-hidden rounded-2xl border border-white/10 bg-slate-900"><div id="territory-map" class="h-[620px] bg-slate-950"></div></section><section class="overflow-hidden rounded-2xl border border-white/10 bg-slate-900"><div class="border-b border-white/10 px-4 py-3 font-semibold">{{ __('Territories') }}</div><div class="max-h-[620px] divide-y divide-white/10 overflow-y-auto">@forelse($rows->sortByDesc('metric_value') as $row)<div class="px-4 py-4"><div class="flex items-center justify-between gap-3"><div><p class="font-semibold">{{ $row['code'] }} · {{ $row['name'] }}</p><p class="mt-1 text-xs text-slate-500">{{ $row['customers'] }} {{ __('customers') }} · {{ $row['visits'] }} {{ __('visits') }}</p></div><div class="text-right"><p class="font-bold text-indigo-200">@if(in_array($metric,['sales','collections'],true)){{ $currency }} @endif{{ number_format($row['metric_value'],2) }}</p><p class="text-[10px] uppercase text-slate-600">{{ __($metric) }}</p></div></div><div class="mt-3 h-1.5 overflow-hidden rounded-full bg-slate-800"><div class="h-full rounded-full bg-indigo-400" style="width:{{ max(3, $row['intensity'] * 100) }}%"></div></div></div>@empty<div class="p-10 text-center text-sm text-slate-500">{{ __('No territory data in this period.') }}</div>@endforelse</div></section></div>
<script src="{{ asset('vendor/leaflet/leaflet.js') }}"></script><script>(()=>{const rows=@json($rows);const map=L.map('territory-map').setView([34.5553,69.2075],10);L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:19,attribution:'&copy; OpenStreetMap contributors'}).addTo(map);const bounds=[];for(const row of rows){if(row.latitude==null||row.longitude==null)continue;const radius=10+(Number(row.intensity)*30);const marker=L.circleMarker([row.latitude,row.longitude],{radius,weight:2,color:'#818cf8',fillColor:'#6366f1',fillOpacity:.18+Number(row.intensity)*.62}).addTo(map);marker.bindPopup(`<strong>${row.code} · ${row.name}</strong><br>${row.customers} customers<br>${row.visits} visits<br>Sales: ${Number(row.sales).toFixed(2)}<br>Collections: ${Number(row.collections).toFixed(2)}`);bounds.push([row.latitude,row.longitude]);}if(bounds.length){map.fitBounds(bounds,{padding:[35,35],maxZoom:13});}})();</script>
</x-layouts.app>