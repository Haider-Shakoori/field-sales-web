<x-layouts.app>
<div class="space-y-5"><div class="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><h1 class="text-3xl font-black">Customers</h1><p class="mt-1 text-sm text-slate-500">Customer master data, route ownership and geofence configuration.</p></div><form><input name="q" value="{{ request('q') }}" placeholder="Search customers…" class="rounded-xl border border-slate-200 bg-white px-4 py-2.5 dark:border-white/10 dark:bg-slate-900"></form></div>
<div class="overflow-x-auto rounded-2xl border border-slate-200 bg-white dark:border-white/10 dark:bg-slate-900"><table class="min-w-full text-sm"><thead class="bg-slate-50 text-left text-xs uppercase tracking-wider text-slate-500 dark:bg-white/5"><tr><th class="p-4">Code</th><th class="p-4">Customer</th><th class="p-4">Phone</th><th class="p-4">Territory / Route</th><th class="p-4">Salesman</th><th class="p-4">Geofence</th><th class="p-4">Status</th></tr></thead><tbody class="divide-y divide-slate-100 dark:divide-white/10">@forelse($rows as $row)<tr><td class="p-4 font-mono text-xs">{{ $row->code }}</td><td class="p-4 font-semibold">{{ $row->shop_name ?: $row->name }}<div class="text-xs font-normal text-slate-500">{{ $row->address }}</div></td><td class="p-4">{{ $row->phone ?? '—' }}</td><td class="p-4">{{ $row->territory?->name ?? '—' }}<div class="text-xs text-slate-500">{{ $row->route?->name ?? 'No route' }}</div></td><td class="p-4">{{ $row->salesman?->user?->name ?? '—' }}</td><td class="p-4">{{ $row->geofence_radius }} m</td><td class="p-4">{{ $row->is_active?'Active':'Inactive' }}</td></tr>@empty<tr><td colspan="7" class="p-10 text-center text-slate-500">No customers found.</td></tr>@endforelse</tbody></table></div>{{ $rows->withQueryString()->links() }}</div>
</x-layouts.app>
