<x-layouts.app>
<div class="mb-6 flex flex-wrap items-start justify-between gap-4"><div><h1 class="text-2xl font-bold tracking-tight">{{ __('Reorder recommendations') }}</h1><p class="mt-1 text-sm text-slate-400">{{ __('Explainable follow-up suggestions based on each customer’s real purchase cadence and recent quantities.') }}</p></div><a href="{{ route('admin.orders.index') }}" class="rounded-xl border border-white/10 bg-slate-900 px-4 py-2.5 text-sm font-semibold text-slate-300 hover:bg-white/5">{{ __('Orders') }}</a></div>
<form method="GET" class="mb-5 flex flex-wrap gap-3 rounded-2xl border border-white/10 bg-slate-900 p-4"><select name="salesman" class="min-w-64 rounded-xl border border-white/10 bg-slate-950 px-3 py-2.5"><option value="">{{ __('All salesmen') }}</option>@foreach($salesmen as $salesman)<option value="{{ $salesman->uuid }}" @selected($selectedSalesman === $salesman->uuid)>{{ $salesman->employee_code }} · {{ $salesman->full_name }}</option>@endforeach</select><button class="rounded-xl bg-indigo-500 px-4 py-2.5 text-sm font-semibold hover:bg-indigo-400">{{ __('Filter') }}</button></form>
<div class="overflow-hidden rounded-2xl border border-white/10 bg-slate-900"><div class="overflow-x-auto"><table class="min-w-full text-left text-sm"><thead class="bg-white/[0.03] text-xs uppercase tracking-wide text-slate-500"><tr><th class="px-5 py-3">{{ __('Customer') }}</th><th class="px-5 py-3">{{ __('Product') }}</th><th class="px-5 py-3">{{ __('Cadence') }}</th><th class="px-5 py-3">{{ __('Suggested qty') }}</th><th class="px-5 py-3">{{ __('Confidence') }}</th><th class="px-5 py-3">{{ __('Reason') }}</th></tr></thead><tbody class="divide-y divide-white/10">@forelse($recommendations as $row)<tr><td class="px-5 py-4"><div class="font-medium">{{ $row['customer_name'] }}</div><span class="mt-1 inline-flex rounded-full px-2 py-0.5 text-[10px] uppercase {{ $row['priority'] === 'overdue' ? 'bg-rose-500/10 text-rose-300' : 'bg-amber-500/10 text-amber-300' }}">{{ __(str($row['priority'])->replace('_', ' ')->title()->toString()) }}</span></td><td class="px-5 py-4"><div class="font-medium">{{ $row['product_name'] }}</div><div class="text-xs text-slate-500">{{ $row['product_sku'] }}</div></td><td class="px-5 py-4 text-slate-300">{{ __('Every ~:days days', ['days' => $row['average_interval_days']]) }}<div class="text-xs text-slate-500">{{ $row['days_since_last_order'] }} {{ __('days since last order') }}</div></td><td class="px-5 py-4 font-semibold">{{ number_format($row['suggested_quantity'], 2) }}</td><td class="px-5 py-4">{{ $row['confidence'] }}%</td><td class="max-w-xl px-5 py-4 text-slate-400">{{ $row['reason'] }}</td></tr>@empty<tr><td colspan="6" class="px-5 py-12 text-center text-slate-500">{{ __('No reorder opportunities are due yet.') }}</td></tr>@endforelse</tbody></table></div></div>
</x-layouts.app>